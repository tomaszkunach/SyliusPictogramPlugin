{% import '@SyliusAdmin/shared/helper/button.html.twig' as button %}

{% set path = options.link.url|default(path(options.link.route, options.link.parameters)) %}

{{ button.default({url: path, class: 'btn-icon', icon: 'cube', text: ''}) }}
